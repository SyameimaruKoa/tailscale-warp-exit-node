# TailscaleのAuth Key（初回認証時のみ必須）
# 認証完了後は ./data_tailscale/ に状態が保存されるため、
# キーが期限切れでもコンテナの再起動に影響しません。
TS_AUTHKEY=tskey-auth-example-key

# Tailscale上での表示名（任意）
# 設定しない場合、自動的に "warp-[サーバーのホスト名]" が使用されます
# TS_HOSTNAME=warp-my-server